[profile.default]
# A hung test should fail the run, not stall it. The live tests talk to a real
# daemon, so the bar allows for a slow answer without allowing an endless one.
slow-timeout = { period = "30s", terminate-after = 4 }
